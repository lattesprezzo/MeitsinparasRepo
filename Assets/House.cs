using UnityEngine;

public class House : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //URpaPESPaskegajsghjoasrg
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
